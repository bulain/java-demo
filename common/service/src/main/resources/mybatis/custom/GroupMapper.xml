<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bulain.common.dao.GroupMapper" >
<!-- custom -->
  <sql id="where">
  	<where>
  		<if test="name != null and name != '' ">
  			and name = #{name}
  		</if>
  	</where>
  </sql>
  <select id="find" resultMap="BaseResultMap" parameterType="com.bulain.common.pojo.GroupSearch">
  	select 
    <include refid="Base_Column_List" />
    from groups
    <include refid="where"/>
    <include refid="common.orderby"/>
  </select>
  <select id="count" resultType="java.lang.Long" parameterType="com.bulain.common.pojo.GroupSearch">
  	select count(*)
    from groups
    <include refid="where"/>
  </select>
  <select id="page" resultMap="BaseResultMap" parameterType="com.bulain.common.pojo.GroupSearch">
  	<![CDATA[${PAGE_HEAD}]]>
  	select 
    <include refid="Base_Column_List" />
    from groups
    <include refid="where"/>
    <include refid="common.orderby"/>
    <![CDATA[${PAGE_FOOT}]]>
  </select>
  <select id="findGroupByLoginId" resultMap="BaseResultMap" parameterType="java.lang.Long" >
  	select g.id, g.name, g.note, g.created_by, g.created_at, g.updated_by, g.updated_at
    from groups g
    join group_logins gl on gl.group_id = g.id
    where gl.login_id = #{loginId}
    order by g.id
  </select>
  <select id="findGroupByNoLoginId" resultMap="BaseResultMap" parameterType="java.lang.Long" >
  	select g.id, g.name, g.note, g.created_by, g.created_at, g.updated_by, g.updated_at
    from groups g
    where not exists (select 1 from group_logins gl where gl.group_id = g.id and gl.login_id = #{loginId})
    order by g.id
  </select>
</mapper>